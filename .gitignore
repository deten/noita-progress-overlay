# Extracted Noita game files. These belong to Nolla Games. Never commit them.
gamedata/

# Build scratch
*.pdb
probe/probe_log.txt

# Screenshots are generated into docs/, everything else is scratch
dist/*.png
!docs/*.png

# Test harness binaries. Only the overlay itself is shipped.
dist/Probe.exe
dist/Simulate.exe
dist/Preview.exe
dist/Shots.exe
