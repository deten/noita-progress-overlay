# Extracted Noita game files. These belong to Nolla Games. Never commit them.
gamedata/

# Build output. Only the overlay itself is shipped; test harness binaries stay local.
dist/*
!dist/NoitaOverlay.exe

# Build scratch
*.pdb
probe/probe_log.txt

# Local drafts, not part of the project
discord-*.txt
